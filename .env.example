# Environment variables for ScrapeVerse Pulse
# Copy this to .env and fill in your values

# Gemini API Key (for AI enrichment — optional, local fallback is used if missing)
GEMINI_API_KEY=your_gemini_api_key_here

# Server port
PORT=3001
